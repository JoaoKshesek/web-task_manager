import React, { FunctionComponent } from "react";

import { SignInContainer } from "@/containers";

const PageSignIn: FunctionComponent = async () => {
  return <SignInContainer />;
};

export default PageSignIn;
